<%= render "shared/header" %>

  <div class="new-article-middle">
    <div class="new-article-side">
    </div>
    <div class="new-article-main">
      <div class="new-article-wrapper">
        <%= form_with model: @article, url: articles_path, local: true, class: "article-form" do |f| %>

          <%= render partial: "shared/error_message", locals: { model: f.object },  class: "error-messages"%>

          <%= f.file_field :image, id: "article_image", class: "article-image" %>
          <%= f.collection_select(:category_id, Category.all, :id, :name, {include_blank: "指定なし"}, class: "article-category") %>
          <%= f.text_field :title, placeholder: "タイトル", class: "article-title" %>
          <%= f.text_area :text, placeholder: "本文", class: "article-text" %>
          <%= f.submit "投稿", class: "article-post", id: "article-post" %>
        <% end %>
      </div>
    </div>
  </div>

<%= render "shared/footer" %>